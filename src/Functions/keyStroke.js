


export const KeyStroke = (addTask) => {

    window.addEventListener("keydown", event => {
        switch (event.keyCode) {
            case 13:

                break;

            default:
                break;
        }
    })
}